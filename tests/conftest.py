"""Shared test setup.

Nothing to configure: `pythonpath = ["src"]` in pyproject.toml makes the
package importable from a plain checkout, which replaced the sys.path
manipulation this file used to perform."""
